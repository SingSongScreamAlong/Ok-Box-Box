{
    "name": "controlbox",
    "version": "1.0.0-rc1",
    "description": "Autonomous Race Control System for Online Leagues",
    "private": true,
    "workspaces": [
        "packages/*",
        "tools/*",
        "apps/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "app": "npm run dev --workspace=@controlbox/dashboard",
        "website": "npm run dev --workspace=@okboxbox/website",
        "relay": "npm run dev --workspace=@okboxbox/relay",
        "api": "npm run dev --workspace=@controlbox/server",
        "verify:preflight": "node scripts/verify-preflight.js",
        "typecheck:dashboard": "npm run typecheck --workspace=@controlbox/dashboard",
        "legacy:website": "node scripts/guard-legacy.js && cd legacy/ProjectBlackBox/blackbox-web && npm run dev",
        "legacy:dashboard": "node scripts/guard-legacy.js && cd legacy/ProjectBlackBox/dashboard && PORT=3005 REACT_APP_BACKEND_URL=http://localhost:3001 npm start",
        "ops:console": "node scripts/guard-legacy.js && cd legacy/ProjectBlackBox/dashboard && PORT=3005 REACT_APP_BACKEND_URL=http://localhost:3001 npm start",
        "test": "npm run test --workspaces --if-present",
        "lint": "npm run lint --workspaces --if-present",
        "typecheck": "npm run typecheck --workspaces --if-present",
        "db:migrate": "npm run migrate --workspace=packages/server",
        "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist tools/*/node_modules tools/*/dist",
        "export:reference": "npx tsx tools/export-reference.ts",
        "export:reference:watch": "npx tsx tools/export-reference.ts --watch"
    },
    "engines": {
        "node": ">=20.0.0"
    },
    "author": "OKBoxBox",
    "license": "MIT"
}